<section class="product-show">
  <h1><%= @product.name %></h1>
  <h2><%= number_to_currency(@product.price_in_cents/100) %></h2>
  <%= image_tag(@product.url, class: "picture") %>
  <p><%= @product.description %></p>
  <p><%= link_to("Edit", edit_product_url) %> | <%= link_to("Delete", product_url(@product.name), method: :delete, data: {confirm: "Are you sure you want to delete #{@product}"}) %>
  </p>
</section>
